<form method="post" action="{{ path('app_build_user_delete', {'id': synergy.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ synergy.id) }}">
    <button class="btn">Delete</button>
</form>
